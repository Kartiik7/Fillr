# ── Fillr Server Environment Variables ─────────────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

PORT=5000
NODE_ENV=development

# MongoDB Atlas connection string
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<database>

# JWT signing secret — generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=<random-64-char-hex-string>

# Google OAuth 2.0 Client ID (from GCP Console → Credentials)
GOOGLE_CLIENT_ID=<your-google-client-id>.apps.googleusercontent.com

# Comma-separated list of allowed CORS origins (no trailing slashes, no spaces)
CORS_ORIGINS=http://127.0.0.1:5500,http://localhost:3000

# Chrome Extension ID — pin CORS to your specific extension (from chrome://extensions)
# Leave blank in development to allow any chrome-extension:// origin
EXTENSION_ID=

# Frontend URL — used in password reset emails and CORS
FRONTEND_URL=https://your-frontend.netlify.app

# Resend (transactional email) — https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxx
RESEND_FROM=YourApp <noreply@yourdomain.com>
